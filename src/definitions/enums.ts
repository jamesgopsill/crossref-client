export enum WorkSortOptions {
	CREATED = "created",
	DEPOSITED = "deposited",
	INDEXED = "indexed",
	IS_REFERENCED_BY_COUNT = "is-referenced-by-count",
	ISSUED = "issued",
	PUBLISHED = "published",
	PUBLISHED_ONLINE = "published-online",
	PUBLISHED_PRINT = "published-print",
	REFERENCES_COUNT = "references-count",
	RELEVANCE = "relevance",
	SCORE = "score",
	UPDATED = "updated",
}

export enum SortOrder {
	ASC = "asc",
	DESC = "desc",
}
